game = {
   frametime = 1 / 60,
   log_level = "Debug" -- Debug, Trace, Info, Warning, Error
}

opengl = {
   debug = true
}

window = {
   width = 1280,
   height = 720,
   title = "FGE",
   fullscreen = false,
   samples = 8,
   vsync = false
}
